<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <!-- Canonical app-icon tile (BRAND.md). Fixed dark squircle; NEVER inverts.
       The frontier disc + sage dot derive from the locked eclipse mark, scaled
       in with padding so the green bump still breaks the silhouette at 16px. -->
  <rect width="100" height="100" rx="22" fill="#1B1A16"/>
  <g transform="translate(50 50) scale(0.82) translate(-50 -50)">
    <circle cx="46" cy="46" r="34" fill="#EDE6D6"/>
    <circle cx="74" cy="74" r="14" fill="#7FB069"/>
  </g>
</svg>
